    <div class="container">

      <div class="starter-template">
        <h1>Suffix Trees</h1>
        <p class="lead">
        Manipulate collections and build trees.
        </p>
      </div>

    </div>
    <div id="exTab3" class="container">
      <ul  class="nav nav-pills">
  			<li class="active"><a  href="#1b" data-toggle="tab">Collections</a></li>
  			<li><a href="#2b" data-toggle="tab">Trees</a></li>
  		</ul>
			<div class="tab-content clearfix">
			  <div class="tab-pane active" id="1b">
          <div class="container">
            <div class="starter-template">
              <form class="form-group">
                <div class="formgroup row col-md-6">
                  <label for="collection">Collection</label>
                  <select class="form-control" id="collection" name="collection" size=5>
                    <option value="foo" selected>foo</option>
                    <option value="bar">bar</option>
                  </select>
                </div>
                <div class="formgroup col-md-6">
                  <label for="collection">Document</label>
                  <select class="form-control" id="document" name="document" size=5>
                    <option value="foo" selected>foo</option>
                    <option value="bar">bar</option>
                  </select>
                </div>
                <div class="formgroup row col-md-12" style="padding-top: 10px;">
                  <div class="textwrapper">
                    <textarea name="data" rows="20"></textarea>
                  </div>
                </div>
              </form>
            </div>
          </div>
				</div>
				<div class="tab-pane" id="2b">
          <div class="container">
            foo
          </div>
				</div>
			</div>
    </div>

    {% if response|length > 0 -%}
    <pre id="reponse">{{ response }}</pre>
    {% endif %}
